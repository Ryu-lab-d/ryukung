# RYUKUNG_BAKERY POS — คำแนะนำถาวรสำหรับ Claude

## กฎสำคัญ: อนิเมชันต้องมาก่อนเสมอ

**ทุกครั้ง**ที่สร้างหน้าใหม่หรือแก้ไข UI ที่ลูกค้าเห็น (โดยเฉพาะหน้า public: `/menu`, `/o/:token`, `/staff/join`)
**ห้ามปล่อยให้องค์ประกอบโผล่มาแบบแข็งๆ ทื่อๆ (ไม่มี transition เลย) เด็ดขาด** ต้องใช้อนิเมชันที่มีอยู่แล้วใน
`src/index.css` เสมอ อย่าเขียน keyframe ใหม่ซ้ำถ้ามีของเดิมที่ใช้ได้อยู่แล้ว — เช็คลิสต์ด้านล่างนี้ก่อนเขียนโค้ด
ทุกครั้งที่งานเกี่ยวข้องกับ UI/อนิเมชัน หรือเมื่อผู้ใช้พูดถึงคำว่า "อนิเมชัน" ไม่ว่ากรณีใด:

| ใช้ตอนไหน | class | หมายเหตุ |
|---|---|---|
| หน้า/การ์ดใหญ่โผล่มาครั้งแรก | `animate-page-in` | เลื่อนขึ้นเบาๆ 10px + fade เร็ว (0.28s) ใช้กับ container หลักของหน้า |
| การ์ด/ฟอร์มที่อยากให้ดูมีชีวิตชีวากว่า page-in | `animate-form-in` | เด้งสปริงเบาๆ (scale+translateY) เห็นชัดกว่า page-in เหมาะกับ section ที่อยากให้สะดุดตา |
| พื้นหลังมัวของป็อปอัพ | `animate-overlay-fade` (เปิด) / `animate-overlay-fade-out` (ปิด) | ใช้คู่กับ toast-pop เสมอ |
| กล่องเนื้อหาป็อปอัพ | `animate-toast-pop` (เปิด) / `animate-toast-pop-out` (ปิด) | เด้งเบาๆ ตอนเปิด |
| ไอคอน/โลโก้ที่อยากให้เด้งเข้ามาแยกจังหวะจากกล่อง | `animate-icon-pop` | มี delay 0.15s ในตัวอยู่แล้ว |
| ของตกแต่งพื้นหลังลอยเบาๆ | `animate-float-slow` | เลิกใช้ในเว็บไซต์ลูกค้าแล้ว (อิโมจิลอยกระจายเต็มจอดูเหมือน AI mockup ไม่ใช่เว็บไซต์จริง) — ใช้ `PageTexture` จาก PublicSiteChrome.tsx แทน |
| section ที่อยู่ใต้จอตอนโหลดหน้า (ต้องเลื่อนถึงจะเห็น) | `<Reveal>` จาก `src/public/PublicSiteChrome.tsx` | **ต้องใช้แทน** `animate-form-in` + `animationDelay` ตรงๆ เสมอสำหรับ section ที่ไม่ได้อยู่บนสุดของหน้า — เล่นอนิเมชันเฉพาะตอน scroll เข้ามาเห็นจริง (IntersectionObserver) ไม่ใช่เล่นครั้งเดียวตอน mount แล้วจบไปเงียบๆ ก่อนผู้ใช้เลื่อนมาเห็น ดูตัวอย่างใน CustomerOrderPage.tsx/AboutTabContent.tsx/PublicOrderPage.tsx — jsdom ไม่มี `IntersectionObserver` เอง ต้อง stub ไว้ (ทำแล้วใน `src/test-setup.ts`) ไม่งั้นเทสต์ที่ mount component ซึ่งใช้ `<Reveal>` จะ throw |
| สลับแท็บ (เมนู ↔ เกี่ยวกับร้าน) | `key={tab}` + `animate-form-in` บน container เนื้อหาที่สลับ | บังคับ remount เนื้อหาทุกครั้งที่สลับแท็บ ทำให้เล่น crossfade ใหม่ทุกครั้ง ดูตัวอย่างใน CustomerOrderPage.tsx |
| สินค้าบินเข้าตะกร้าตอนกดเพิ่ม | `flyToCart()` จาก `src/public/PublicSiteChrome.tsx` | ใช้ Web Animations API สร้างเส้นโค้งจากปุ่มที่กดไปยังปุ่มตะกร้าลอยมุมขวาล่าง (`CartFab`) — **ห้าม**ใช้ป็อปอัพ/กล่องขาวโผล่กลางจอตอนเพิ่มสินค้าลงตะกร้าเด็ดขาด (ดูกฎถัดไป) |
| แสงอุ่นเคลื่อนไหวช้าๆ ตลอดเวลาบน hero | `<AmbientGlow />` จาก `src/public/PublicSiteChrome.tsx` | วนลูป 14s subtle มาก ต้องอยู่ใน container ที่มี `relative overflow-hidden` เสมอ กันแสงล้นออกนอก hero |
| การ์ดสินค้ากระเด้งตอนกดเพิ่มลงตะกร้า | `animate-cart-bump` | คู่กับเสียง `playAddSound()` จาก `src/lib/uiSound.ts` เสมอ |
| กรอกผิด/แจ้งเตือน error | `animate-shake`, `animate-field-error` | |
| Toast แจ้งผลสำเร็จ/สถานะ | `animate-status-toast` | |
| ป้ายชวนคุยข้างปุ่มแชท | `animate-chat-nudge`, `animate-chat-ring` | |
| หน้าจอ "กำลังโหลด" | `animate-loading-bounce`, `animate-loading-dot` | |
| ระฆัง/เสียงประกาศ | `animate-bell-ring`, `animate-sound-ring` | |
| โลโก้เต็มจอตอนเปิดหน้าครั้งแรก (splash) ก่อนหดเล็กจางหายเผยหน้าเว็บ | `animate-splash-shrink` | ค้างตัวใหญ่ 0.6s แรกแล้วหด+จางใน 0.45s ที่เหลือ (รวม 1.1s) ใช้กับหน้า public ที่อยากได้ความรู้สึกเปิดแบรนด์ |

**ทำให้หลาย section โผล่ทีละจังหวะ (staggered)**: ใส่ `style={{ animationDelay: '0.1s' }}` ควบคู่กับ class
ข้างต้น เพิ่มค่า delay ทีละนิดในแต่ละ section ถัดไป (เช่น 0s, 0.12s, 0.22s) — อย่าให้ทุกอย่างโผล่มาพร้อมกันหมด
ทีเดียวเพราะจะดูแข็งและไม่มีมิติ

## กฎสำคัญ: ห้ามซ้อนเนื้อหาซ้ำกันระหว่างป็อปอัพกับหน้าเพจ

ถ้าเนื้อหาบางอย่าง (เช่น เรื่องราวร้าน "เกี่ยวกับร้าน") มีอยู่ถาวรในหน้าเพจอยู่แล้ว **ห้ามทำป็อปอัพที่โชว์
เนื้อหาเดียวกันซ้ำแบบบังคับเปิดทันทีตอนเข้าเพจ** เพราะจะไปบังของที่เพิ่งสร้างไว้ให้สวยๆ ไม่ให้ผู้ใช้เห็นเลย —
ถ้าจะมีป็อปอัพเสริม ให้เป็นแบบกดเปิดเองเท่านั้น (manual trigger) ไม่ auto-show ทับเนื้อหาเพจที่มีอยู่แล้ว

## กฎสำคัญ: ห้ามมีอะไรโผล่ตรงกลางจอตอนกดเพิ่มสินค้าลงตะกร้า

ฟีดแบ็กตอนกด "เพิ่มลงตะกร้า" ต้อง**ไม่ใช่**ป็อปอัพ/กล่องขาว/toast โผล่กลางจอเด็ดขาด (เคยโดนบ่นว่า "อะไรสีขาวๆ
ก็ขึ้นมาเฉยๆ มันไม่ใช่ไง") ให้ใช้ `flyToCart()` (สินค้าบินโค้งเข้าปุ่มตะกร้า) + `animate-cart-bump` บนปุ่มตะกร้าเอง
เท่านั้น — ตะกร้าเองก็ต้องเป็นปุ่มลอยเดียวมุมขวาล่าง (`CartFab`) ไม่ใช่แถบเต็มความกว้างโผล่กลาง/ล่างจอ

## โทนสี/ดีไซน์ที่ใช้อยู่ในระบบ (อย่าออกนอกกรอบนี้โดยไม่จำเป็น)

- พาเลตหลัก: Tailwind `stone` (bg-stone-50/900, text-stone-500/700/900, border-stone-200/300)
- สีแบรนด์ร้าน (hero/ป็อปอัพแนะนำร้าน): gradient `linear-gradient(160deg, #3d2b1f, #6b4a35)` ตัวหนังสือขาว
- ปุ่มหลัก (primary action): `bg-stone-900 text-white rounded-xl` (หรือ `rounded-lg` ถ้าเล็กกว่า)
- ปุ่มรอง: `border-2 border-stone-200 text-stone-700` หรือ `bg-stone-100`
- **ปุ่ม/ลิงก์ที่กดได้ทุกอันต้องมีหน้าตาเป็น "ปุ่มจริง" เสมอ — ห้ามเป็นแค่ตัวหนังสือขีดเส้นใต้ลอยๆ (`text-sm underline`
  เปล่าๆ) เด็ดขาด** โดยเฉพาะปุ่มย้อนกลับ/นำทางระหว่างขั้นตอน (เช่น "← กลับไปแก้ตะกร้า", "← กลับไปแก้ข้อมูล") เคย
  ทำเป็นตัวหนังสือขีดเส้นใต้ธรรมดาแล้วโดนบ่นว่าไม่สวย ให้ใช้ `BackButton` component ที่มีอยู่แล้ว (ปุ่มทรงแคปซูล
  มีขอบ `border border-stone-300 bg-white`) ใน `src/public/CustomerOrderPage.tsx` เสมอสำหรับปุ่มย้อนกลับ — ถ้า
  จะเพิ่มปุ่มใหม่ที่ไหนในเว็บไซต์ลูกค้า ให้ใช้สไตล์ปุ่มหลัก/ปุ่มรองด้านบนนี้เท่านั้น ลิงก์ขีดเส้นใต้ (`underline`)
  ใช้ได้เฉพาะลิงก์เนื้อหาที่ฝังอยู่ในย่อหน้าข้อความจริงๆ เท่านั้น ไม่ใช่กับปุ่มกระทำการ (action)
- ปุ่ม/ลิงก์ไลน์: สีเขียวไลน์แท้ `#06C755` เท่านั้น (ทั้งตัวหนังสือ+กรอบ หรือพื้นหลัง ขึ้นกับบริบท)
- การ์ด: `rounded-xl` หรือ `rounded-2xl` (ป็อปอัพใหญ่ใช้ `rounded-3xl`) พร้อม `shadow-sm` ถึง `shadow-2xl` ตามความสำคัญ — ในเว็บไซต์ลูกค้าใช้เงาอุ่นแบบ custom แทน `shadow-sm` เปล่าๆ: `shadow-[0_2px_16px_-6px_rgb(51_32_14_/_0.18)]` + `border border-stone-200/70` ให้ดูมีมิติ/พรีเมียมกว่าเงาเทาแบนๆ
- มุมโค้งเพิ่มขึ้นตามขนาด/ความสำคัญของ element (ปุ่มเล็ก < การ์ด < ป็อปอัพเต็มจอ)

## ฟอนต์/ดีไซน์ "อบอุ่นงานฝีมือ" เฉพาะเว็บไซต์ลูกค้า (public site: /menu, /about)

ทิศทางดีไซน์ที่เลือกไว้คือ "อบอุ่นงานฝีมือ" (warm artisan) — **ไม่ใช่** flat gradient+อิโมจิลอยกระจาย
+ ฟอนต์ default ของเบราว์เซอร์แบบเดิม (แบบนั้นดูเหมือนเว็บ AI generate มากกว่าเว็บไซต์ร้านจริง) กฎตายตัว:

- ห่อ container หลักสุดของทุกหน้า public (เว็บไซต์ลูกค้า) ด้วย class `font-warm` เสมอ (ฟอนต์ Sarabun) แล้วใส่
  `font-display` (ฟอนต์ Mitr) เฉพาะหัวข้อ (h1/h2/ชื่อร้าน) — ฟอนต์ทั้งสองนี้ scope เฉพาะเว็บไซต์ลูกค้าเท่านั้น
  **ห้ามเอาไปใช้ในหน้า backend/POS ที่พนักงานใช้** (ไม่ได้อยู่ใน scope ของดีไซน์นี้ อย่าเปลี่ยนฟอนต์ default ทั้งระบบ)
- พื้นหลังของทุกหน้า public ใช้ `<PageTexture />` จาก `src/public/PublicSiteChrome.tsx` (ลายกระดาษคราฟท์จางๆ)
  **ห้าม**กลับไปใช้อิโมจิลอยกระจายพื้นหลังแบบเดิม (FloatingDecor เดิมถูกลบไปแล้วเพราะดูเหมือน mockup ไม่ใช่เว็บจริง)
- ใช้ `<SquiggleUnderline />` (เส้นขีดใต้ลายมือ) ใต้หัวข้อ hero หลักของแต่ละหน้าเพื่อความรู้สึกงานฝีมือ — อย่าใช้พร่ำเพรื่อ
  เกินหัวข้อใหญ่ๆ ของแต่ละหน้า (ไม่งั้นจะดูรก)
- Section ที่ไม่ได้อยู่บนสุดของหน้าต้องห่อด้วย `<Reveal>` (ดูตารางอนิเมชันด้านบน) ไม่ใช่ `animate-form-in` ตรงๆ
- อิโมจิใช้แบบเจาะจง (1 อันต่อ section/หัวข้อ) ไม่ใช่ของตกแต่งลอยเกลื่อนพื้นหลัง

## โครงสร้างเว็บไซต์ลูกค้า: หน้าเดียว สลับด้วยแท็บ (ไม่ใช่หลายหน้าแยก route)

`/menu` กับ `/about` เป็น **หน้าเดียวกัน** (component เดียวคือ `CustomerOrderPage.tsx`) สลับเนื้อหาด้วย query param
`?tab=menu|about` ผ่าน `useSearchParams` — ไม่ remount ทั้งหน้า ไม่กระเด้งไปมา รู้สึกเหมือนกดแท็บจริงๆ (ผู้ใช้เคย
บ่นว่าทำเป็นหน้าแยกแล้วมันดูแยกจากกันเกินไป ขอแค่ปุ่มสลับแท็บสวยๆ) เส้นทาง `/about` เป็นแค่ redirect ไป
`/menu?tab=about` เผื่อมีลิงก์เก่าอ้างถึง **ถ้าจะเพิ่มแท็บใหม่ในอนาคต ห้ามสร้าง route แยก** ให้ทำตาม pattern นี้แทน

## จุดที่ควรอ่านเพิ่มก่อนแก้หน้า public

- `src/public/PublicOrderPage.tsx` — หน้าติดตามออเดอร์ลูกค้า (`/o/:token`) ย้ายมาใช้ชุดดีไซน์ "อบอุ่นงานฝีมือ" เดียวกับ /menu แล้ว (font-warm/font-display, PageTexture, CARD shadow style, bg-brand-shader บน popup แนะนำร้าน, Reveal บน card ที่อาจอยู่ใต้จอ) ไฟล์นี้มี logic ธุรกิจเยอะมาก (ยืนยันตัวตน/สถานะชำระเงิน/timeline) แก้เฉพาะส่วนดีไซน์เท่านั้น อย่าแตะ logic โดยไม่จำเป็น
- `src/public/CustomerOrderPage.tsx` — เว็บไซต์ลูกค้าทั้งหมด (`/menu` และ `/about` ผ่าน tab param) ประกอบด้วย hero แบรนด์ร่วม + เนื้อหาแท็บเมนู (ค้นหา/หมวด/ตะกร้า) + แท็บเกี่ยวกับร้าน (`AboutTabContent`) + ขั้นตอนสั่งซื้อ 4 step เรียงกัน: `menu` (เลือกสินค้า) → `review` (ทวนรายการ+แก้จำนวนได้อีกรอบ) → `checkout` (กรอกที่อยู่ → เจอ `RememberNamePopup` บังคับติ๊กว่าจำชื่อ/เบอร์ได้ก่อนไปต่อ) → `payment` (สแกน QR+ยืนยัน) — ปุ่มย้อนกลับทุกขั้นตอนต้องใช้ `BackButton` (ดูกฎปุ่มด้านบน) ห้ามข้าม step หรือเพิ่ม step ใหม่โดยไม่รักษาลำดับนี้
- `src/public/AboutTabContent.tsx` — เนื้อหาแท็บ "เกี่ยวกับร้าน" ล้วนๆ (ไม่มี chrome ของตัวเอง ถูกฝังใน CustomerOrderPage.tsx) เจ้าของร้าน/เส้นทางการเติบโต/จุดเด่น — เนื้อหาจริงต้องอิงข้อมูลที่เจ้าของร้านให้มาเท่านั้น ห้ามแต่งข้อเท็จจริงเอง (ชื่อ/ปี/เหตุการณ์) ถ้าไม่มีข้อมูลให้ถามก่อน
- `src/public/PublicSiteChrome.tsx` — ของที่ใช้ร่วมกันทุกหน้าเว็บไซต์ลูกค้า: `PageTexture` (พื้นหลังลายกระดาษ), `PublicNav` (แถบนำทาง+แท็บสลับ+ปุ่มวิธีสั่งซื้อ), `PublicFooter`, `Reveal` (ระบบโผล่ตอน scroll), `SquiggleUnderline`, `WaveDivider` (ขอบคลื่นคั่น hero), `AmbientGlow` (แสงเคลื่อนไหวช้าๆ), `CartFab` + `flyToCart()` (ตะกร้าลอย+อนิเมชันบินเข้าตะกร้า) — เพิ่มหน้า/ฟีเจอร์ public ใหม่ให้ import จากตรงนี้แทนก็อปโค้ดซ้ำ ดูหัวข้อ "ฟอนต์/ดีไซน์ อบอุ่นงานฝีมือ" ด้านบนก่อนแก้ไฟล์นี้เสมอ
- `src/index.css` — รวม keyframe/utility อนิเมชันทั้งหมดของระบบ เช็คก่อนเขียนใหม่เสมอ

## กฎสำคัญ: submit_customer_order ต้องเรียกผ่าน Edge Function เท่านั้น ห้าม .rpc() ตรงๆ เด็ดขาด

`submit_customer_order` (ออเดอร์ที่ลูกค้าสั่งเองจาก /menu) ถูกล็อกไม่ให้ `anon`/`authenticated`/`public` เรียก
ตรงผ่าน PostgREST ได้อีกต่อไป (ดู migration `20260926090500`–`20260926090700`) — เรียกได้เฉพาะ `service_role`
เท่านั้น ของจริงต้องผ่าน Edge Function `submit-customer-order` (ดู `supabase/functions/submit-customer-order/`)
ที่ตรวจ Cloudflare Turnstile token ก่อนเสมอถึงจะยอมเรียก RPC ให้ — กันบอท/สคริปต์ยิง order ปลอมเข้าระบบตรงๆ
โดยข้าม CAPTCHA (เจอจริงว่าไม่มี Turnstile แล้วบอทสร้างออเดอร์ปลอมได้ไม่จำกัดมาก่อนแก้)

**ถ้าจะเพิ่มฟังก์ชัน `security definer` ใหม่ที่ anon เรียกได้จากหน้า public**: Postgres ให้สิทธิ์ `EXECUTE` กับ
`PUBLIC` โดยอัตโนมัติทุกครั้งที่สร้างฟังก์ชันใหม่ — แค่ `grant ... to anon` เฉยๆ **ไม่พอ** ถ้าอยากล็อกให้เรียกได้
เฉพาะทางที่ต้องการ (เช่นผ่าน Edge Function เท่านั้น) ต้อง `revoke execute ... from public` ด้วยเสมอ ไม่งั้น
`anon` จะยังเรียกผ่านสิทธิ์ PUBLIC ที่ติดมาแต่แรกได้อยู่ดี (นี่คือบั๊กจริงที่เจอตอนล็อก submit_customer_order —
revoke จาก anon/authenticated ไปแล้วแต่ลืม public เลยยังเรียกตรงได้เหมือนเดิม) ทุกครั้งที่แก้ grant/revoke ต้อง
ยิง `notify pgrst, 'reload schema';` ตามท้ายเสมอ แล้วทดสอบด้วย `curl` ตรงๆ ไปที่ `/rest/v1/rpc/<ชื่อฟังก์ชัน>`
จริงๆ (ไม่ใช่แค่เชื่อเทสต์ supabase-js เฉยๆ) เพื่อยืนยันว่า permission denied จริง ก่อนจะเชื่อว่าล็อกสำเร็จ

- `src/public/TurnstileWidget.tsx` — component ป้ายยืนยันไม่ใช่บอท โหมด managed (ผู้ใช้จริงส่วนใหญ่ผ่านอัตโนมัติ)
  ใช้ `VITE_TURNSTILE_SITE_KEY` (public, อยู่ใน .env — ปลอดภัยที่จะเห็นในหน้าเว็บ) ส่วน secret คู่กันเก็บแยกไว้ที่
  Supabase Edge Function secret `TURNSTILE_SECRET_KEY` เท่านั้น (`supabase secrets set`) ห้ามเอา secret มาไว้ที่
  frontend เด็ดขาด
- `tests/db/customer-self-order.test.ts` — เทสต์ตรรกะธุรกิจของ `submit_customer_order` ต้องเรียกผ่าน
  `adminClient()` (service role) ไม่ใช่ `anonClient()` แล้ว (เพราะ RPC ล็อกไว้) มีเทสต์แยกยืนยันด้วยว่า
  anon/authenticated เรียกตรงไม่ได้จริง — ถ้าจะเพิ่มเทสต์ RPC ที่ยิงไปสร้างออเดอร์ลูกค้าใหม่ ให้เรียกผ่าน
  adminClient() ตามแพทเทิร์นนี้เสมอ
